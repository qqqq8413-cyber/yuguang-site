# 嶼光映像 PHOS OF ISLE · 官網專案說明

> 本文件整理官網的專案背景、技術架構、後台與資料、UI/UX 設計系統、開發流程與待辦。
> 最後更新：2026-09-23（對應 PR #1–#43）

---

## 1. 專案概要

| 項目 | 內容 |
|---|---|
| 品牌 | 嶼光映像 PHOS OF ISLE（屏東） |
| 服務 | 平面攝影、動態影像製作、影像器材日租 |
| 正式網址 | https://phosofisle.com/ （網域於 Cloudflare 註冊，DNS 在 Cloudflare；舊網址 super-bombolone-186f83.netlify.app 301 轉到正式網域） |
| 後台 | https://phosofisle.com/後台 |
| 原始碼 | GitHub `qqqq8413-cyber/yuguang-site`（`main` 分支即正式站） |
| 主機 | Netlify（靜態網站 + Functions + Forms + Deploy Preview） |
| 流量統計 | Cloudflare Web Analytics（Cloudflare 控制台 → Analytics → Web analytics → phosofisle.com） |

**網站的三個任務**
1. 用作品說服客戶（平面作品、動態作品）
2. 讓器材租借從「詢價」變成「直接送出租借需求」
3. 讓站長不碰程式碼就能更新內容與處理訂單（/後台）

---

## 2. 技術架構

```
訪客瀏覽器
  │  靜態 HTML/CSS/JS（無前端框架、無打包工具；部署時有一個建置步驟 build-pages.js 產生單頁與 sitemap）
  ▼
Netlify ──────────────┬─ 靜態檔案：yuguang-site/
                      ├─ Functions：netlify/functions/*.js
                      └─ Forms：rental-inquiry（租借需求備援）、聯絡表單
        │                     │                    │
        ▼                     ▼                    ▼
   GitHub API            Airtable API        Cloudinary
 （內容 JSON 存檔）       （租借訂單）       （照片儲存與轉檔）
```

- **沒有資料庫**：網站內容（相簿、影片、器材、網站資訊）是 `yuguang-site/content/*.json`，後台存檔時經由 GitHub API 寫回 repo，Netlify 偵測到新 commit 後自動重新部署（約 1 分鐘）。
- **訂單**存在 Airtable；同時送一份到 Netlify Forms 當備援。
- **照片**存在 Cloudinary，網址直接寫在 JSON 裡；顯示時用 Cloudinary 網址參數即時轉檔（尺寸、格式、裁邊、AI 放大）。
- **影片**放在 YouTube，網站只存影片 ID，播放用內嵌播放器。

### 2.1 目錄結構

```
/
├─ PROJECT.md                 ← 本文件（不會被發布到網站）
├─ netlify/functions/         ← 後端函式（Node）
└─ yuguang-site/              ← 網站根目錄（Netlify 發布此資料夾）
   ├─ index.html              首頁
   ├─ pingmian.html           平面作品
   ├─ dongtai.html            動態作品
   ├─ qicai.html              器材租賃
   ├─ liucheng.html           製作流程
   ├─ guanyu.html             關於嶼光
   ├─ lianluo.html            聯絡我們
   ├─ 後台.html               後台（訂單、器材、照片、影片）
   ├─ 內容.html / 訂單.html    舊網址，自動轉到 /後台
   ├─ assets/site.css         全站共用樣式（導覽、頁尾、預約區塊、對比修正）
   ├─ assets/site.js          全站共用腳本（導覽列、手機選單、頁尾資料、淡入保險）
   ├─ assets/gear-icons/*.svg 器材分類線稿圖示（沒有照片時使用）
   ├─ content/*.json          網站內容資料
   ├─ images/                 og.jpg（分享預覽圖）、網站圖示、logo
   ├─ favicon.svg / favicon-32.png / apple-touch-icon.png / site.webmanifest
   ├─ assets/vendor/          第三方程式放自己主機（Sortable 1.15.0，與 cdnjs 官方檔案 SHA-512 相同）
   └─ _headers                Netlify 標頭設定（全站安全標頭、CSP Report-Only、manifest 的 Content-Type）
```

### 2.2 頁面與網址規則

| 頁面 | 檔案 | 可分享的網址參數 |
|---|---|---|
| 首頁 | `index.html` | — |
| 平面作品 | `pingmian.html` | 點進作品後網址變成 `/work/<分類>/<專案>/`（＝單一作品頁的網址）；舊的 `?cat=&proj=`、`?album=&project=` 仍可開啟並自動換成新網址 |
| 動態作品 | `dongtai.html` | `?cat=分類名稱`；點進影片後網址變成 `/video/<影片ID 小寫>/` |
| 器材租賃 | `qicai.html` | 點進器材後網址變成 `/rental/<器材代稱>/`；舊的 `?item=` 仍可開啟 |
| 製作流程 | `liucheng.html` | — |
| 常見問題 | `wenda.html` | —（價格區間、交件、修改、訂金、改期、器材租借規則；含 FAQPage 結構化資料） |
| 租借須知 | `xuzhi.html` | —（租期、取還、證件與本票、發票、取消改期、逾期、損壞賠償；不放在導覽列，從器材頁與每個器材單頁連進去） |
| 關於嶼光 | `guanyu.html` | — |
| 聯絡我們 | `lianluo.html` | `?type=服務類型`（預選需求標籤） |
| 後台 | `後台.html` | `#orders`、`#order-<訂單ID>`、`#gear`、`#photo`、`#video` |

---

## 3. 內容資料（content/*.json）

| 檔案 | 用途 | 主要欄位 |
|---|---|---|
| `albums.json` | 平面作品 | `albums[]`：`zh`、`en`、`sub`（副標題）、`slug`、`cover`、`featured`（首頁精選）、`photos[]`、`projects[]`（專案也有 `sub`、`slug` 與 `featured`）；照片 `image`、`caption`、`w`、`h` |
| `videos.json` | 動態作品 | `videos[]`：`title`、`cat`、`yt`、`client`、`year`（作品年份，顯示用）、`publishedAt`（YouTube 上傳日期，給 Google 用）、`featured`、`cover`、`sub`、`desc`、`credits` |
| `gear.json` | 器材 | `gear[]`：`name`、`slug`、`cat`、`price`（日租）、`qty`（可租數量，0＝暫停出租）、`image`、`spec`（用「・」分隔）、`desc`、`uses`（每行一項）、`note`（租借說明）；`cats[]` 分類順序 |
| `site.json` | 網站資訊 | 品牌名、Email、LINE、電話、地址、社群連結 `instagram`／`facebook`／`youtube`／`google`（頁尾與聯絡頁讀這裡，也輸出到 LocalBusiness 的 sameAs） |
| `about.json` / `process.json` | 關於、流程頁內容 | `about.json` 另有 `founded`、`facts[]`（工作室事實）、`clients[]`（合作單位），部署時寫進關於頁 |

目前規模：平面 7 個分類、455 張照片；動態 14 部影片；器材 9 項。

**網址代稱（`slug`）**：分類、專案、器材各有一個固定的英文代稱，網址用它而不是排列序號，所以後台拖曳排序、改中文名稱都不會讓分享出去的連結跑掉。後台留空時存檔會自動產生（英文名稱轉小寫；沒有英文就用隨機碼），也可以手動修改——但改了代稱，舊的分享連結就會失效。**代稱是必填**：格式檢查會擋下缺代稱的內容（後台存檔、PR 檢查、部署建置三處都擋），建置程式也不再用排列順序產生備用網址。

**照片寬高（`w`、`h`）**：每張照片都記錄原始尺寸，頁面載入時先保留正確比例的空間，照片載入後版面不會跳動。後台上傳時自動寫入。

---

## 4. 後台（/後台）

登入：輸入站長密碼（`ADMIN_PASSWORD`），同一個分頁內保持登入。

| 分頁 | 功能 |
|---|---|
| 器材租賃 · 訂單 → 租借訂單 | 統計（全部／待處理／已確認／租借中）、依狀態篩選、精簡列表；點進單筆看完整需求、複製 LINE ID／用 LINE 加好友、改狀態、刪除 |
| 器材租賃 · 訂單 → 器材管理 | 依分類管理器材：名稱、分類、日租價、可租數量、規格、介紹、推薦用途、租借說明、照片；拖曳排序；分類新增／改名／刪除 |
| 平面作品 · 照片 | 分類 → 專案 → 照片；多張上傳、設封面、拖曳排序（分類封面可從各專案的照片裡挑） |
| 動態作品 · 影片 | 依分類管理；YouTube ID、客戶、年份、精選、自訂封面、簡介、製作團隊 |

**訂單狀態**：待處理 → 已報價 → 已確認 → 租借中 → 已歸還（或 已取消）。
不租了建議改「已取消」保留紀錄；刪除會從 Airtable 直接移除。

**防止資料被蓋掉**：後台從 GitHub 讀取最新內容並記住版本（sha）；存檔時若 GitHub 上已有更新的版本（例如另一個分頁存過），會拒絕並提示重新整理，不會默默覆蓋。未存檔就離開頁面會跳出提醒。

**照片上傳**：直接從瀏覽器上傳到 Cloudinary（簽名上傳，簽名由 `sign-upload` 函式產生，只有登入後台才拿得到）。**拿不到簽名就停止上傳**（fail closed），不會退回未簽名的方式；Cloudinary 上也不保留任何未簽名的上傳設定。後台不載入外部 CDN 的程式。上傳器材照片後，會在背景先產生介紹頁用的 AI 放大版。

---

## 5. 後端函式（netlify/functions）

所有後台用函式都呼叫同一個驗證模組 **`lib/auth.js`**（`requireAdmin`），不各自比對密碼：
- `x-admin-key` 標頭要等於 `ADMIN_PASSWORD`；先雜湊再用 `timingSafeEqual` 比對，回應時間不會洩漏密碼
- **同一來源連續輸錯 5 次鎖 15 分鐘**（回 429 與 `Retry-After`），鎖定期間連正確密碼也不接受；每次輸錯多等 0.8 秒；輸對一次歸零；沒帶密碼的請求只回 401、不算一次嘗試
- 後台登入畫面會顯示「再錯 N 次會暫時鎖住」與「請約 N 分鐘後再試」
- 限制：錯誤次數記在函式執行個體的記憶體，Netlify 可能同時有多個執行個體、閒置後也會重啟，所以是「盡力而為」的減速；真正的防線仍是夠長的密碼
- 新增後台函式時，第一行就要 `const denied = await requireAdmin(event); if (denied) return denied;`（自動測試會檢查）

| 函式 | 呼叫者 | 作用 |
|---|---|---|
| `create-order.js` | 器材頁（公開） | 新增租借訂單到 Airtable。**只收代稱＋數量＋日期＋姓名＋LINE**，器材名稱、單價、天數、預估金額一律由後端依部署時打包的 `gear.json` 計算（`lib/rental.js`），前端送來的金額與天數會被忽略；不存在、暫停出租、超過可租數量、日期不合理都回 400 並附錯誤代碼。蜜罐欄位擋機器人 |
| `list-orders.js` | 後台 | 讀取全部訂單（自動翻頁，不受 100 筆限制） |
| `update-order.js` | 後台 | 更新訂單狀態（只接受 6 種狀態、檢查訂單 ID 格式） |
| `delete-order.js` | 後台 | 刪除訂單 |
| `get-content.js` | 後台 | 從 GitHub 讀取內容 JSON 與版本（sha） |
| `save-content.js` | 後台 | 寫回內容 JSON 到 GitHub；只允許 `yuguang-site/content/*.json`；版本不符回 409 |
| `sign-upload.js` | 後台 | 產生 Cloudinary 簽名上傳參數 |

**預覽版只能看（`lib/context.js`）**：部署預覽（`deploy-preview-N--….netlify.app`）與分支部署的後台**不能寫入正式資料**——存檔、改訂單、刪訂單、上傳照片都會回 403 並提示到正式網址操作；讀取仍可用，方便在預覽上檢查畫面。後台頁面本身也會顯示橘色橫幅並提前擋下這些動作。
> 為什麼：後台的存檔是直接寫進正式資料（GitHub 主線、Airtable、Cloudinary）。2026-09-23 在預覽版後台勾了「首頁精選」，寫進了正式站當時還不認得的欄位，導致正式站部署連續失敗、網站停在舊版。

**租借報價（`lib/rental.js`）**：訂單金額的唯一依據。規則與器材頁相同：可租數量未填＝1、0＝暫停出租；天數含取件日與歸還日；起租日不可早於台灣時間的昨天（給時區較晚的訪客寬限一天）；租期上限 365 天、一張單最多 30 種器材；未定價的器材不計入金額並標「價格另計」。後台改價格會觸發重新部署，函式裡的價格跟著更新。

**租借規則（`xuzhi.html`）**：實際營運的條款，內容**寫死在頁面裡**（不是 `content/*.json`，後台改不到；要改條款請直接改這個檔）。現行規定：
- 租期含取件日與歸還日，最短一天
- **取件時押一張證件（身分證或健保卡）並簽立本票**，歸還點驗無誤後當場返還；保管期間不影印、不拍照、不留存副本
- 取還在屏東工作室當面進行，**週一至週五 11:00–20:00**（與首頁 JSON-LD 的營業時間一致，改一邊要改兩邊），不提供宅配
- 器材頁價格**未稅**，需統編發票另加 5%（器材單頁的 `Offer` 也標了 `valueAddedTaxIncluded: false`）
- **取消與改期一律免費**，只要事先告知；天災免費改期
- 未告知而逾期：每逾 1 天加收當日租金 **1.5 倍**；逾期超過 7 天無法聯繫依本票處理
- 損壞依實際維修費＋維修期間租金損失（上限 7 天）；無法修復或遺失依當時市價

送出租借需求前必須勾選「我已閱讀並同意租借須知」，`create-order` 收到 `agree !== true` 會回 400 `terms-not-agreed`（前端擋一次、後端再擋一次）。**同意紀錄目前沒有寫進 Airtable**：要留存的話，得先自己在 Airtable 加一個欄位，再改 `create-order.js` 的 `fields`，否則 Airtable 會因為不認得欄位而整筆失敗。

**內容格式檢查**：`netlify/functions/lib/content-schema.js` 定義每份內容 JSON 允許的欄位與型別（欄位打錯字、價格填成文字、網址格式錯、代稱重複都會被抓到）。後台存檔時 `save-content` 先檢查，不通過回 422 並列出問題，後台會把位置翻成器材／相簿名稱顯示。

### 5.1 環境變數（在 Netlify 設定，只記名稱）

| 名稱 | 用途 |
|---|---|
| `ADMIN_PASSWORD` | 後台密碼 |
| `GITHUB_TOKEN` | 讀寫 repo 內容 JSON（需 contents 讀寫權限） |
| `GITHUB_REPO`、`GITHUB_BRANCH` | 選填，預設 `qqqq8413-cyber/yuguang-site`、`main` |
| `AIRTABLE_TOKEN`、`AIRTABLE_BASE_ID` | 訂單資料庫 |
| `AIRTABLE_TABLE_NAME` | 訂單資料表名稱（目前為「匯入表 2」） |
| `CLOUDINARY_API_KEY`、`CLOUDINARY_API_SECRET` | 簽名上傳 |
| `CLOUDINARY_CLOUD_NAME` | 選填，預設 `wy6xqh42` |

> 密碼與金鑰一律由站長自行在 Netlify 設定，不寫進程式碼、不經他人之手。

### 5.2 Airtable 訂單欄位

`客戶姓名`、`聯絡方式`（單行文字，例：`LINE abc123`）、`租借器材`（例：`KUPO 090 燈架 ×2（NT$50/日）、Canon EOS R50 ×1（NT$300/日）`）、`起租日`、`迄租日`、`天數`、`預估金額`、`狀態`。

> `聯絡方式` 必須是文字欄位；曾經是數字欄位，導致開頭的 0 被吃掉、文字被清空。

---

## 6. UI 設計系統

### 6.1 風格定位
米白紙感 × 墨黑文字 × 襯線中文字。像一本攝影集：留白多、裝飾少，讓作品是主角。

### 6.2 色彩

| Token | 值 | 用途 |
|---|---|---|
| `--paper` | `#f3f1ec` | 頁面底色 |
| `--paper-2` | `#eeece6` | 次要底色（統計卡、說明框、總額區） |
| `--ink` | `#171613` | 主要文字、主要按鈕 |
| `--muted` | `#716a5d` | 次要文字（由原本 `#a19a8d` 加深，對比 2.5:1 → 4.7:1，符合 WCAG AA） |
| `--line` | `rgba(23,22,19,.14)` | 分隔線、框線 |
| `--hair` | `rgba(23,22,19,.08)` | 極淡分隔線 |
| 錯誤 | `#b04a3a` | 表單錯誤、刪除按鈕 |

訂單狀態標籤各有一組淡底深字配色（待處理＝橘、已報價＝藍、已確認＝綠、租借中＝紫、已歸還＝灰、已取消＝紅）。

### 6.3 字體
- 標題與品牌：`Noto Serif TC`（Google Fonts，400／500），後備 `Songti TC`、Georgia
- 內文與表單：系統字 `PingFang TC`、`Noto Sans TC`
- 字距偏寬（標題 3–16px），營造雜誌感
- 字型以非阻塞方式載入；照片多的頁面（首頁、平面作品）等第一屏照片載完才載入網頁字型，避免跟照片搶頻寬

### 6.4 共用元件
| 元件 | 說明 |
|---|---|
| 頂欄導覽 | 平面作品／動態作品／器材租賃／製作流程／關於嶼光＋「聯絡我們」按鈕；900px 以下收成漢堡選單（全螢幕米白覆蓋） |
| 首頁頂欄 | 桌面在主視覺上為透明、淺色字，捲過主視覺轉米白底；手機直式固定米白底 |
| 預約區塊（sitecta） | 作品頁／器材頁底部的「預約諮詢」引導 |
| 全站頁尾 | 品牌、Email、LINE、電話、地址，內容讀 `site.json` |
| 淡入 | 0.45 秒、位移 10px；尊重「減少動態效果」設定；有保險機制避免內容卡在隱形狀態 |
| 器材替代圖 | 沒有照片時顯示紙色底＋分類線稿圖示 |

### 6.5 響應式斷點（主要）
- `≤ 520px`：手機窄版細節（租借浮動列只顯示件數）
- `≤ 600px`：器材卡片兩欄、名稱單行省略
- `≤ 640px`：動態作品改單欄清單、後台表單單欄
- `≤ 900px`：導覽列收成漢堡選單
- `max-aspect-ratio: 4/5`（直式螢幕）：首頁主視覺改 16:9、文字疊在影像上、按鈕移到影像下方

---

## 7. 各頁 UX 設計決策

### 首頁
- 滿版主視覺輪播：精選影片中有自訂封面的優先，封面用 `e_trim` 自動裁掉電影黑邊；不足兩張時補平面作品
- 桌面：品牌 Logo＋名稱在左側垂直置中，「瀏覽作品／預約諮詢」按鈕在左下，與品牌區分開；底部顯示正在播放的作品名，可點進該作品
- 手機直式：16:9 影像、文字疊在影像上、兩顆按鈕放影像下方（經多輪調整後定案）
- 第一屏即可看到導覽（原本捲過主視覺才出現）
- 下方三列橫向捲動：平面作品、動態作品（大卡片）、器材租賃（正方形去背商品卡，名稱在照片下方）
- **平面作品那一排＝後台勾選的「★ 首頁精選」**（分類有專案時勾專案；沒有專案時勾分類本身）。一個都沒勾時顯示全部，所以不會突然變空。順序依後台的分類／專案排序，切換後立刻存檔
- 動態作品那一排＝後台勾選的「★ 精選影片」
- 每張卡片直接連到該作品／器材的網址
- 從作品按上一頁回首頁時，還原原本捲動位置

### 平面作品
- 分類 → 專案 → 照片三層；照片點開為放大檢視（左右切換、Esc 關閉）
- 效能：`srcset` 依螢幕挑適當尺寸、延遲載入、第一屏提高下載優先權、模糊預覽圖（LQIP）先顯示
- 實測（4G）：92 張相簿從 94 個請求／13.3MB 降到約 8 個請求／1.1MB；第一屏清晰時間 2.5 秒 → 1.19 秒；版面跳動 1227px → 0

### 動態作品
- 「精選作品」預設分類 + 各分類篩選；電腦版為不規則拼貼格
- 手機版改單欄清單：影像在上、標題在下，分類列固定在頂欄下方
- 作品詳情：YouTube 播放器、副標題、簡介、製作團隊 credit、客戶與年份

### 器材租賃（購物車式）
- 取消「詢價」：客人選器材 → 選日期 → 填姓名與 LINE ID → 直接送出租借需求
- 卡片直接顯示「NT$ ○○○ / 日」；未定價顯示「價格另計」；可租數量 0 顯示「暫停出租」
- 加入後按鈕變「已加入 ✓」，再按一次開啟清單
- **底部浮動列**：有器材才滑出，顯示件數與日租合計；加入時彈跳一下並短暫顯示「已加入租借清單」
- **租借清單**：畫面中央彈出視窗；每項可調數量（上限為可租數量）與小計；起訖日期不可選過去、迄日不可早於起日
- **清單會留著**：選好的器材與日期存在瀏覽器本機（`localStorage`，7 天），回首頁、看作品、關掉分頁再回來都還在；存的是器材代稱不是順序編號，所以後台調整排序也不會對到錯的器材。送出後自動清空；已下架或暫停出租的品項還原時會自動略過
- **即時試算**：日租合計、租期（含頭尾，10/1 取 10/2 還＝2 天）、預估總額
- 必填：姓名、LINE ID（手機號碼於後續聯絡時再取得）
- 送出：先交給後端（`create-order`）重新計算並寫入 Airtable，成功後再留一份 Netlify 表單備份（用後端算的金額）
  - 後端判定內容有誤（器材下架、數量超過、日期不對…）→ 顯示原因、不送出，清單保留
  - 後端連不上或 Airtable 出錯 → 改用 Netlify 表單備援，訂單不會遺失
  - 兩邊都失敗 → 提示重試或直接加 LINE
- **介紹頁**：介紹、推薦用途、規格重點、租借說明（例：amaran 300c 附贈 KUPO 090 燈架一支）；數量選擇與「共 N 組可租」
- **器材照片**：站長上傳的是去背 PNG；網站用 `e_trim` 裁掉透明邊、完整顯示在頁面底色上（不裁切、不加白底）；介紹頁大圖用 AI 放大（`e_upscale`），放大後再用原圖去背形狀（`fl_cutter`）剪一次以保留透明；放大前先縮到 1000px 以內，避免超過 Cloudinary 25MP 上限
- 名稱、規格過長以「…」截斷，卡片對齊；完整內容在介紹頁

### 聯絡我們
- 需求類型改為標籤單選；從其他頁帶 `?type=` 可預選
- 電話、希望日期為選填；錯誤就地提示；只有伺服器回應成功才顯示送出成功
- LINE 連結格式：`https://line.me/R/ti/p/~<ID>`（不含 @）

### 瀏覽歷史（上一頁）規則
- 每一層都寫進網址，可分享、可重新整理
- 上一頁一次只退一步：先關視窗（租借清單、照片放大圖）→ 回列表（還原分類與捲動位置）→ 最後才離開頁面
- 從分享連結直接進入作品時，頁面上的「返回列表」會新增一筆紀錄，之後按上一頁可以回到該作品
- 後台訂單詳情同樣適用（`#order-<id>`）

### 主要頁面的靜態內容（部署時寫入）
7 個主要頁面的內容原本全靠 JS 從 `content/*.json` 載入。Google 會執行 JS，但 **AI 搜尋的爬蟲（GPTBot、ClaudeBot、PerplexityBot）不會**，它們看到的「關於嶼光」只有標題。而且首頁的靜態 HTML 完全沒有連到 43 個作品／器材頁。

`scripts/prerender-main.js`（部署時在 `build-pages.js` 之後執行）把這些內容寫進 HTML：
- 每頁固定位置放 `<!--pre:名稱-->` … `<!--/pre:名稱-->`，程式整段取代，可重複執行結果相同
- **導覽列與頁尾**：7 頁都有真正的 `<a>`（`site.js` 偵測到已存在就不重畫）
- **作品清單**：平面／動態／器材頁底部列出所有 `/work/`、`/video/`、`/rental/` 連結，依分類分組
- **關於、流程、聯絡**：寫進 JS 本來就會填的同一個容器，JS 載入後換成同樣內容，畫面不會重複（頁面 JS 已改成先清空容器）
- **`html.no-js`**：`<head>` 的第一行 script 會移除它；沒有 JS 時用 CSS 讓 `.reveal` 區塊直接顯示，內容才看得到
- 不執行 JS 時的文字量：首頁 186、平面 319、動態 408、器材 505、流程 535、關於 551、聯絡 428 字（原本 30–90 字）

### 單一作品／器材頁（部署時產生）
`scripts/build-pages.js` 在每次 Netlify 部署時，從 `content/*.json` 產生 43 個獨立頁面與 `sitemap.xml`：
- `/video/<影片ID 小寫>/`、`/work/<分類>/<專案>/`、`/work/<分類>/`、`/rental/<器材代稱>/`
- 每頁有自己的標題、描述、分享預覽圖與結構化資料（VideoObject／ImageGallery／CollectionPage／Product）
  - 影片：`uploadDate` 取自 `publishedAt`（YouTube 上傳日期；Google 要有它才會把頁面當影片結果顯示）。沒填就不放，不拿建置日期假裝
  - 器材：`Offer` 標明 `businessFunction = LeaseOut`（出租，不是出售），價格是每 1 天的租金（`unitCode = DAY`）
- sitemap：不放 `lastmod`（沒有每頁真正的修改日期，每次部署都填今天會讓 Google 不再相信這個欄位）；照片只放 `image:loc`（`image:title`／`image:caption` 已被 Google 停用）
- 產生的檔案不進版控（見 `.gitignore`）；本機預覽先跑 `node scripts/build-pages.js`
- **建置前先檢查內容格式**（與後台存檔同一份規則）；不通過就中止、不產生任何頁面，Netlify 會保留上一版網站，壞網址不會發布出去
- 網址只由代稱與影片 ID 決定；影片 ID 只差大小寫時第二支加 `-2`，「更多作品」連結與頁面本身一致
- 部署設定在 `netlify.toml`（build command 與 publish 資料夾）
- 注意：Netlify 網址不分大小寫，所以影片頁路徑一律小寫
- 站內點進作品時，網址列直接換成單一作品頁的網址（pushState），所以使用者複製到的永遠是可分享的那一個；重新整理則由伺服器送出該靜態頁

### 結構化資料（SEO）
首頁 `<head>` 有 JSON-LD：`WebSite` 與 `LocalBusiness`＋`ProfessionalService`（聯絡方式、屏東地址、服務區域、營業時間、價格級距、成立年份、`sameAs` 社群）；`wenda.html` 有 `FAQPage`（16 題）。內容與 `content/site.json` 相同，**修改聯絡資料時兩邊都要改**。

### 安全標頭（`_headers`）
- 全站：`X-Content-Type-Options: nosniff`、`Referrer-Policy: strict-origin-when-cross-origin`（YouTube 內嵌需要來源網域，不能用 no-referrer）、`Permissions-Policy` 關閉相機／麥克風／定位／付款／USB、`X-Frame-Options: SAMEORIGIN`（防止後台被嵌進別的網站）
- **內容安全政策目前是 Report-Only（只回報、不阻擋）**。允許的外部來源：Google Fonts、Cloudinary（圖片與上傳 API）、YouTube（縮圖與內嵌）、Cloudflare Web Analytics，以及 Netlify 只在部署預覽插入的工具列（`app.netlify.com`）。上線前已在本機與部署預覽逛遍全站與後台各分頁，0 違規
- 因為內嵌 `<script>`／`<style>`／`onclick` 還很多，暫時需要 `'unsafe-inline'`
- **新增外部服務時**（新字型、新圖片來源、新的嵌入或統計），要把網域加進 CSP 對應項目；改成正式 CSP 之後，沒加的會被瀏覽器擋掉
- 下一步：觀察一段時間 → 改成正式的 `Content-Security-Policy`

### 把資料放進 HTML（後台訂單的 XSS，PR #44 修掉）
後台會把客人在器材頁自己填的 LINE ID 顯示出來。原本的寫法是把它組進行內事件：

```html
<button onclick="copyText('資料放這裡',this)">複製 LINE ID</button>
```

只要資料裡有一個單引號，就會跳出 JS 字串，**後面的內容會以站長的身分執行**（可讀走 `sessionStorage` 的登入密碼，接著改內容、刪訂單）。屬性層的 `&#39;` 擋不住：瀏覽器會先解碼屬性值，才把內容交給 JS。

現在的規則：
- **客人填的內容一律不進行內事件**。要帶值就放 `data-*` 屬性，用一個掛在 `document` 上的事件委派讀 `dataset`（後台的 `data-copy`／`data-vcat`／`data-gcat`）
- `create-order.js` 用白名單擋在最前面：LINE ID 只收 `@` 開頭選填＋英數與 `. - _`、4–20 字（LINE 本身的規則），不合格回 400 `invalid-line-id`，器材頁送出前也先檢查一次
- `esc()` 多跳脫單引號，當作第二層保險
- `check-scripts.js` 會擋下再次把字串資料組進 `onclick` 的寫法（CI 會失敗）
- 之後 CSP 改成正式版、拿掉 `'unsafe-inline'`，才是把這一整類問題關起來

### 公開端點的節流（`lib/ratelimit.js`）
`create-order` 是任何人都能打的端點，Airtable 免費方案有筆數上限、Netlify 表單每月有提交上限，被灌垃圾單會在最需要的時候剛好用完。現在同一來源（IP）**10 分鐘內最多 5 筆**，超過回 429（附 `Retry-After`），器材頁顯示「送出得太頻繁」且**不走表單備援**。內容有誤（日期、數量）被退回的那幾次不算進額度，客人修正重送不會被擋。次數記在函式執行個體的記憶體裡（同 `lib/auth.js`），Netlify 會開多個執行個體、閒置後重啟，所以這是拖慢洗版而不是硬上限。

### 頂欄與手機選單（PR #46 修正）
- 頂欄的排版寫在 `assets/site.css`：桌機用格線 `1fr auto`，**logo 靠左、導覽列靠右**。以前是 flex 的 `space-between`，有「← 返回首頁」這個第三個元素時導覽列會被擠到中間，拿掉又會跑到別的位置；改成格線之後，左右有幾個元素都不影響。
- **頁尾一定是頁面的最後一塊**。作品頁／器材頁底部的預約區塊（`.sitecta`）是 `site.js` 產生的，以前用 `appendChild` 加到 body 最後；#42 之後頁尾變成部署時就寫進 HTML，於是預約區塊被排到頁尾**後面**。現在改成插在頁尾之前，檢查也會驗「預約區塊在頁尾之前，且頁尾後面沒有其他內容」。
- **頁尾只能有一個**。`site.js` 會補頁尾，頁面裡多留一個舊的就會變成兩個——首頁在 #42 之後同時留著舊的 `<footer>© …</footer>` 與新的 `.sitefoot`，畫面上就出現兩個頁尾。現在 `buildFooter()` 看到已有的 `.sitefoot` 會沿用，`check-build.js` 也會數 `<footer>` 的個數。
- 各頁面的 `<style>`（以及產生頁共用的 `assets/page.css`）**不可以定義沒有前綴的 `.cta`**：導覽列的「聯絡我們」按鈕就是 `a.cta`，會被誤中。常見問題頁曾因此讓頂欄從 65px 變成 198px，`page.css` 也讓 **43 個產生的作品／器材頁**頂欄變成 153px（頁面自己的 `.cta` 帶了上下 28–80px 的 margin）。頁面自己的按鈕群組請用 `.pagecta`。`check-build.js` 會擋下這兩件事。
- **手機選單鈕由 `assets/site.js` 產生**。PR #42 把導覽列寫進 HTML 之後，原本的程式碼「看到已經有導覽列就直接 return」，結果**所有主要頁面在手機上都沒有選單鈕**，手機使用者只能靠 logo 回首頁（2026-09-23 上線～2026-09-24 修正）。現在不論導覽列是預先寫好的還是 JS 補的，都會補上選單鈕。
- **靜態清單（`<!--pre:list-->`）必須放在多欄版面的外面**。器材頁的 `.catalog` 是「側欄＋器材格」兩欄，#42 把靜態清單插在它裡面變成第三個子元素，於是被排進側欄那一欄、和分類選單疊在一起（1980px 寬最明顯）。已移到 `.catalog` 之外，並在 `site.css` 給 `.sitelinks` 加上 `grid-column:1/-1;width:100%` 當保險。
- 手機的可點範圍：`site.css` 最後一段把頁尾聯絡方式、麵包屑、作品清單、logo 等只有 17–25px 高的連結撐到 37px 以上（只加 padding，必要時用等量負邊距抵銷，視覺不變）。

### 封面怎麼決定
- **專案封面**：進「管理照片」後，每張照片下方都有「設為封面」（再按一次取消）。
- **分類封面**：分類詳情頁上方的「分類封面」區，按「選擇封面」會列出這個分類底下**所有專案的照片**，點任一張即可。以前只有分類自己有照片時才選得到，底下只有專案的分類（例如人像寫真、活動紀錄）完全沒有入口。
- **代表圖的順序（首頁、平面作品頁、產生的頁面與 og:image 四處一致）**：自己的 `cover` → 自己的第一張照片 → **第一個專案的 `cover`** → 第一個專案的第一張照片。
  > 2026-09-24 修正：以前分類自動抓圖時會跳過專案的封面、直接用專案的第一張照片。站長在「婚禮紀錄」的專案裡換了封面，分類卡片卻沒變，就是這個原因。首頁的卡片輪播也改成封面排第一張。
- 指定封面後，分類卡片就不會再輪播多張圖（`gatherImgs()` 看到 `cover` 會回空陣列），這是刻意的：你挑的那張就是要固定顯示的那張。

### 動態（PR #49、#50）
全站共用一套節奏，寫在 `assets/site.css` 的 `--ease-out`／`--ease-in`／`--ease-inout`：進場用 expo-out（快速到位、慢慢停），離場比進場短。只動 `transform` 與 `opacity`（不影響版面、不造成跳動）。

**速度只改一個地方：`site.css` 的 `--motion-scale`**（目前 1.4；1 = 最初的節奏，數字越大越慢）。捲動浮現（`site.js` 讀這個值）、首頁進站動畫與主視覺浮現、換頁、導覽列底線、手機選單全部跟著變。關上手機選單、跳過進站動畫這類「離場」刻意不跟著變慢。

| 位置 | 動態 | 寫在哪 |
|---|---|---|
| 首頁・進站 | 深色畫面上用光把 LOGO 一筆一筆畫出來（先畫地平線、再升起三道直線、最後射出那道光），名字浮現，接著像暗房顯影一樣淡出成主視覺，約 2.2 秒 | `index.html`（`<head>` 的 `/*intro*/` 腳本＋`.intro` 樣式＋`</body>` 前的收尾腳本） |
| 首頁・主視覺 | LOGO、名字、標語、按鈕、輪播資訊依序浮上來；播進站動畫時整體接在顯影之後 | `index.html` 的 `hero-in`（純 CSS，第一次繪製就套用） |
| 作品卡片、照片、器材、問答 | 捲到時一張一張浮現：同一批依位置（上→下、左→右）排序，每張間隔 65ms、最多排 8 張；有外框的圖片從 1.1 倍縮回 1 倍。點進分類、切換篩選時新卡片也依序浮現 | `site.js` 的 `motion()`（Web Animations，不改元素的 CSS，hover 照常） |
| 首頁每一列（可橫向滑動） | 整列捲進畫面時由左到右依序浮現，右邊還沒滑到的也一起出現（不會等你橫向滑到才出現） | 同上的 `scrollerOf()` |
| 換頁 | 舊頁淡出、新頁淡入並微微上浮，頂欄固定不動（Chrome／Edge／Safari 18.2+；其他瀏覽器照常換頁） | `site.css` 的 `@view-transition` |
| 桌機導覽列 | 滑過時底線從左畫出、離開時往右收 | `site.css` |
| 手機選單 | 打開時連結由上往下依序浮現；關上時一起淡出 | `site.css` |

**進站動畫的規則**：每次開瀏覽器後第一次進首頁才播（`sessionStorage` 的 `yg-intro`）；點一下、按鍵、滾動就立刻跳過（0.32 秒淡出），主視覺文字同時改用一般的時間點；系統設定「減少動態」、網址帶 `#錨點`、自動化工具都不播。**網址加 `?intro=1` 可強制重看**（「減少動態」仍然優先）。結束不只靠 `animationend`，另有 2.5 秒計時保底。

**捲動浮現的規則**：只有 `site.js` 會把還沒捲到的元素加上 `.rv-wait`（隱藏），所以沒有 JS 時內容全部可見；列印時 `.rv-wait` 一律顯示。要讓新的區塊也有這個效果，把它的選擇器加進 `site.js` 的 `REVEAL_SEL`。

**為什麼剛載入時第一屏不做動畫（`calm`）**：Google 的「最大內容繪製」（LCP，排名會看）只量使用者第一次操作之前。卡片若在這時淡入，Chrome 要等動畫播完才算畫出來——實測平面作品頁從 0.66 秒變成 1.55 秒。所以第一次點擊／按鍵／滾輪／觸控之前，第一屏直接顯示（從別頁點進來時有換頁淡入）；之後出現的才依序浮現。淡入的起點也用透明度 0.01 而不是 0（肉眼看不出差別，Chrome 會算成已畫出）。改動畫前後請量 LCP，不要只看畫面。

**「減少動態」**：系統設定開啟時，進站動畫、捲動浮現、主視覺浮現、換頁動畫、選單動畫全部關閉，內容直接出現。

> ⚠️ **`<html>` 上的旗標不可以和元素的 class 同名。** 進站動畫第一版把旗標叫 `intro`、遮罩也叫 `.intro`，結果 `.intro{display:none}` 連 `<html>` 一起隱藏，整頁在動畫結束前完全沒畫出來（首次繪製從約 50ms 變成 2.5 秒）。現在旗標是 `is-intro`／`is-intro-hero`。

### 內容資料寫進頁面（PR #50，修版面位移 CLS）
首頁、平面作品、動態作品、器材租賃、關於，以前都是頁面載入後才 `fetch('content/*.json')`，資料回來前作品格是空的，底下的靜態清單與頁尾先出現在畫面裡，資料一到就被整塊推下去——**手機版面位移 CLS 最高 0.53**（Google 標準 0.1 以下才算好）。

現在 `scripts/prerender-main.js` 在部署時把用到的 JSON 寫進頁面的 `<!--pre:data-->` 區塊（`<script type="application/json" id="yg-data-…">`），並提供 `window.ygFetch`：有內嵌資料就直接給、沒有就照舊 `fetch`。頁面程式寫成 `(window.ygFetch||fetch)('content/…')`，所以沒跑建置時也不會壞。另外 `site.css` 讓還沒畫出來的作品格（`#grid`／`#albumGrid`／`#rows` 是 `:empty` 時）先佔一個畫面高，防止瀏覽器在頁面程式執行前先畫一次。

結果（每種 5 次取最大值，資料延遲 0.8 秒模擬手機網路）：15 種「頁面 × 寬度」CLS 全部 0（之前 8 種超過 0.1）；手機 LCP 平面作品 1.45→0.66 秒、動態作品 1.38→0.63 秒。`check-scripts.js` 會驗證內嵌資料是合法 JSON，`check-build.js` 會擋下空的 `pre:data` 區塊。

> **git 裡的 `pre:data` 區塊刻意留空**（不像導覽列、清單那些區塊是填好的）：資料是 content/*.json 的完整副本，放進 git 只會變成過期的內容與動輒上百 KB 的差異。Netlify 與 CI 都會先跑 `prerender-main.js` 再檢查，所以部署出去的頁面一定有資料。本機要看完整效果請先跑 `node scripts/build-pages.js && node scripts/prerender-main.js`，**跑完後不要把填好的 `pre:data` 提交進去**。

### 流量統計
- 使用 **Cloudflare Web Analytics**：不用 Cookie、不追蹤個人，因此不需要 Cookie 同意視窗
- 網域是灰色雲朵（DNS only），所以採**手動 JS snippet**（Cloudflare 設定為「Enable with JS Snippet installation」），不是自動注入
- 載入點只有一處：`assets/site.js` 最後一段。所有公開頁（含部署時產生的作品／影片／器材頁）都載入 site.js；後台不載入，站長自己的操作不計入
- 只在 `phosofisle.com`／`www.phosofisle.com` 啟用，本機與部署預覽不送資料
- 站內切換網址（pushState 到 `/work/`、`/rental/`、`/video/`）也會記成一次瀏覽（已用測試確認）
- 限制：只看得到瀏覽量、熱門頁面、來源、裝置與國家、載入速度；**看不到按鈕點擊**（例如「加入租借」）。實際送出的租借需求以 Airtable 為準
- token 是公開識別碼（本來就出現在網頁原始碼），不是密碼

### 分享預覽
7 個公開頁面都有 Open Graph／Twitter 卡片標籤、頁面描述、網站圖示與 PWA manifest；分享圖為 `images/og.jpg`。

---

## 8. 開發流程

1. 從 `main` 開分支（例：`gear-qty`）
2. 修改、在本機以無頭 Chrome 測試（手機 390×844、桌面 1280×800）
3. 開 Pull Request → Netlify 自動產生預覽網址：
   `https://deploy-preview-<PR 編號>--super-bombolone-186f83.netlify.app/<頁面>`
4. 站長用手機／電腦看預覽確認 → **由站長自己合併**
5. 合併後約 1 分鐘上線

**注意事項**
- 不直接推送到 `main`
- 後台存檔會直接 commit 到 `main`：若某個 PR 也改到 `content/*.json`，請先合併 PR 再用後台編輯，或把 `main` 合併進 PR 分支
- Commit 與 PR 說明使用中文，描述「改了什麼、為什麼」

### 8.1 測試方式
- **自動檢查（GitHub Actions，`.github/workflows/check.yml`）**：每個 PR 與 main 都會跑
  - `node scripts/validate-content.js`：內容 JSON 格式
  - 每個後端函式都能載入
  - `node scripts/test-preview-guard.js`：預覽版唯讀（27 項：4 支寫入函式在預覽／分支部署回 403、讀取仍可用、正式站與本機不受影響、後台頁面也會先擋）
  - `node scripts/test-admin-auth.js`：後台驗證（28 項：6 支函式對錯密碼、錯 5 次鎖定、鎖定中正確密碼也拒絕、15 分鐘解鎖、不同來源互不影響、每支函式都用共用驗證）
  - `node scripts/test-create-order.js`：租借報價與下單函式（52 項：金額天數由後端算、偽造金額被忽略、數量／日期／品項各種錯誤、Airtable 失敗與未設定、**LINE ID 白名單**、**同來源節流**、**須知勾選**）
  - `node scripts/build-pages.js`：與 Netlify 部署相同的建置；失敗代表合併後會部署失敗
  - `node scripts/prerender-main.js`：與部署相同，把主要頁面的靜態內容寫進 HTML
  - `node scripts/check-build.js`：產生的頁面 canonical／og:url 正確、標題與 h1、網址格式；sitemap 網址都存在、無重複、沒漏頁；**把順序反過來並改掉所有中文名稱後，網址必須完全相同**；產生的檔案沒被 commit；**7 個主要頁面不執行 JS 時仍有導覽列、足夠文字與作品連結**；**標題重複只提醒（△）**；器材標明出租與按日計價、sitemap 沒有 lastmod 與停用欄位；影片缺上傳日期只提醒（△）不擋
  - `node scripts/check-scripts.js`：每個頁面（含產生的頁面）的內嵌程式與 assets/*.js 沒有語法錯誤，結構化資料是合法 JSON，**行內事件（`onclick="…"`）的 JS 字串裡沒有插入資料**（見下方「把資料放進 HTML」）
  - `node scripts/check-links.js`：網站內部連結（含產生的頁面，約 750 個）都指到存在的檔案
- **上一頁回歸測試**：38 個情境（首頁、動態、平面、器材、分享連結進入），桌面與手機各跑一次
- **租借流程**：本機接上真的 `create-order` 函式（Airtable 以假回應代替），測正常送出、後端拒絕（顯示原因）、後端掛掉改走表單、兩邊都失敗、只有表單失敗
- **後台訂單**：模擬 Airtable 回應，測列表 → 詳情 → 改狀態 → 上一頁 → 刪除
- **函式**：以 Node 模擬請求，測密碼錯誤 401、ID 格式錯誤 400、正常流程
- **效能**：無頭 Chrome 模擬 4G，量請求數、下載量、第一屏時間、版面跳動

---

## 9. 變更歷程

| PR | 內容 |
|---|---|
| #1 | UI 第一階段：全站導覽、淡入加速、文字對比 |
| #2 | UI 第二階段：首頁滿版主視覺 |
| #3 | UI 第三階段：預約區塊、全站頁尾、器材替代圖 |
| #4 | 聯絡表單：修正送出判斷、需求類型改成標籤、欄位提示 |
| #5 | 相簿頁：延遲載入真正生效，手機流量從 13MB 降到 1MB |
| #6 | 相簿照片記錄長寬：載入時版面不再跳動 |
| #7 | 動態作品：手機版改成單欄清單 |
| #8 | 分享預覽、頁面描述與網站圖示 |
| #9 | 刪除沒有用到的大圖（13.6MB） |
| #10 | 器材上架：換成真實器材 9 項 |
| #11 | 首頁：平面／動態作品卡片放大 |
| #12 | 後台：修正會默默蓋掉修改的問題，並強化後端函式 |
| #13 | Cloudinary 改用簽名上傳；刪除舊的 /admin 後台 |
| #14 | 後台上傳失敗時顯示真正原因 |
| #15 | 後台整合成一頁：訂單、器材、照片、影片都在 /後台 |
| #16 | 器材租賃：租借清單直接送出、即時天數與總額、只收姓名＋LINE、介紹頁分段、底部浮動列、中央視窗、文字截斷 |
| #17 | 器材租賃：數量選擇＋後台可租數量 |
| #18 | 首頁第一屏導覽列、作品頁與器材頁的上一頁行為、首頁捲動位置還原 |
| #19 | 後台訂單：單筆詳情＋刪除 |
| #20 | 器材照片完整顯示（保留去背）＋介紹頁 AI 放大 |
| #21 | 新增本文件 PROJECT.md |
| #22 | 正式網域 phosofisle.com、舊網址 301 轉址、robots.txt、sitemap.xml |
| #23 | 品牌風格的 404 頁面 |
| #24 | 內容格式檢查：後台存檔前驗證、PR 自動檢查（內容／函式／程式語法／內部連結） |
| #25 | 穩定網址：相簿、專案、器材改用固定代稱（slug） |
| #26 | 手機器材名稱兩行、移除舊上傳函式 |
| #27 | 首頁結構化資料（WebSite＋LocalBusiness） |
| #28 | 每個作品、器材各自產生獨立頁面（分享預覽＋SEO），sitemap 7 → 50 筆 |
| #29 | sitemap 加入 462 張照片（Google 圖片搜尋） |
| #30 | 租借清單暫存：離開頁面再回來不會被清空 |
| #31 | 流量統計：Cloudflare Web Analytics（僅正式網域、後台不計入） |
| #32 | 租借訂單改由後端計算金額與天數（前端只送代稱、數量、日期），附 36 項自動測試 |
| #33 | PR 檢查跑正式建置並檢查產生的頁面與 sitemap；代稱改為必填，建置不再產生依順序編號的網址 |
| #34 | SEO 語意：影片補 YouTube 上傳日期（後台可填）、器材標明出租與按日計價、sitemap 移除假日期與停用欄位 |
| #35 | 安全清理：Cloudinary 上傳 fail closed（移除未簽名備援）、Sortable 改自家主機、全站安全標頭與 CSP Report-Only |
| #36 | 後台驗證：6 支函式共用 `lib/auth.js`、防計時攻擊比對、連續輸錯 5 次鎖 15 分鐘，登入畫面顯示剩餘次數 |
| #37 | 首頁「平面作品」改由後台勾選「★ 首頁精選」決定（沒勾時顯示全部） |
| #38 | SEO 關鍵字標題：7 個主頁面與相簿／器材單頁的 `<title>`、og:title 加入「屏東攝影／影片製作／器材租借」等搜尋詞（畫面上的 h1 不變） |
| #39 | 預覽版後台改為唯讀：存檔／上傳／改訂單只能在正式網址做 |
| #40 | 修正 #38 的標題：相簿不再出現「照片攝影」、專案頁補上「屏東＋分類」、器材頁後綴縮短；檢查程式會提醒重複標題 |
| #41 | 相簿與專案可填「副標題」：同名作品用它區分，顯示在標題下方，也進搜尋結果標題 |
| #42 | 主要 7 頁在部署時寫入靜態內容：導覽列、作品清單、關於／流程／聯絡（給不執行 JS 的 AI 爬蟲讀） |
| #43 | 常見問題頁（16 題：價格、交件、修改、訂金、器材租借規則）＋關於頁工作室事實與合作單位＋LocalBusiness 補齊營業時間／價格級距／社群 |
| #44 | 修掉後台訂單詳情的 XSS：客人填的 LINE ID 不再進 `onclick`（改 data 屬性＋事件委派）、後端加 LINE ID 白名單、`create-order` 同來源節流、CI 擋下同類寫法 |
| #45 | 租借須知頁（`xuzhi.html`）＋送出前必須勾選同意（前後端都驗）＋器材頁與器材單頁標明未稅與取還規則；常見問題的押金說明改為「押證件」 |
| #46 | 頂欄一致：拿掉「返回首頁」、導覽列改格線靠右、修好常見問題頁被 `.cta` 撞高的頂欄；**補回手機選單鈕**（#42 之後所有主要頁面在手機上沒有選單）；手機可點範圍加大 |
| #47 | 修掉首頁重複的頁尾、產生頁頂欄被 `.cta` 撞高（43 頁）、器材頁靜態清單疊到分類側欄、預約區塊被排到頁尾後面；檢查加入「頁尾只能一個」與 page.css 的 `.cta` |
| #48 | 後台可以挑「分類封面」（從該分類底下所有專案的照片裡選）；並修正**分類自動抓圖會跳過專案封面**的問題（首頁、平面作品頁、產生頁、og:image 四處） |
| #49 | 全站動態：首頁進站 LOGO 動畫（光畫出 LOGO → 顯影成主視覺）、作品卡片捲動依序浮現、換頁淡入淡出、導覽列與手機選單動態；全部尊重「減少動態」 |
| #50 | 動畫整體放慢（新增全站速度倍率 `--motion-scale`，目前 1.4）；修版面位移：內容資料在部署時寫進頁面、空的作品格先預留高度，CLS 全部降到 0，手機 LCP 快約 0.8 秒 |

---

## 10. 待辦與建議

### 優先
- [x] **綁定自有網域**：phosofisle.com（2026-09-20）
- [x] **sitemap.xml 與 robots.txt**
- [x] **Google Search Console**：驗證網域、提交 sitemap（7 頁已探索）
- [x] ~~首頁主視覺封面換掉有字幕的畫面~~：站長決定維持原樣（2026-09-22）
- [x] **流量統計**：Cloudflare Web Analytics（PR #31）

- [x] **每個作品、器材各自產生 HTML 頁面**（PR #28）

### 一般
- [x] 自訂 404 頁面
- [x] 器材「可租數量」：未填即 1 台，只有多台的器材需要填（站長確認）
- [x] 手機器材卡名稱改為最多 2 行
- [x] 移除不再使用的 `upload-image.js`

### 架構審查後續（2026-09-22，每項一個 PR）
- [x] 租借訂單金額與天數由後端計算（PR #32）
- [x] PR 檢查也跑 `build-pages.js`，檢查產生的頁面、連結與 sitemap；代稱（slug）改為必填（PR #33）
- [x] SEO 語意：影片補上傳日期、器材標明「出租」、移除假的 sitemap 更新日期、圖片 sitemap 只留網址（PR #34）
- [x] 安全清理：移除 Cloudinary 未簽名備援程式（preset `yuguang` 已刪）、Sortable 改自家主機、基本安全標頭＋CSP Report-Only（PR #35）
- [x] 後台驗證：6 支後台函式共用一個驗證模組、密碼比對防計時攻擊、錯誤次數限制（PR #36）
- [ ] CSP 從 Report-Only 改為正式（觀察一段時間無違規後）
- [ ] 之後再評估：後台登入改 HttpOnly cookie

### 之後
- [ ] 後台：影片分類改名／刪除
- [ ] Airtable 資料表「匯入表 2」改名，並同步更新 `AIRTABLE_TABLE_NAME`
- [ ] 訂單加「未成交原因」欄位，累積定價與採購依據
- [ ] 器材 360° 旋轉圖（先挑一台試做）
- [ ] 手機選單開啟時按上一頁改為關閉選單（目前會離開頁面，與多數網站一致）

### 已知限制
- 在 YouTube 內嵌播放器內點其他影片，上一頁會先在播放器內退回（YouTube 行為，網站無法控制）
- 後台存檔後約 1 分鐘才會出現在網站上（Netlify 重新部署時間）
- 預覽版後台只能看，不能改（要修改內容請用正式網址的後台）
- 刪除訂單無法從後台復原
